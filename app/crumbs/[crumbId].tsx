import { Text, View } from "react-native";

export default function SkillCrumbPage() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Edit app/[skillCrumbId].tsx to edit this screen.</Text>
    </View>
  );
}
